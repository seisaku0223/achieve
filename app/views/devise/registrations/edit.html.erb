<div class="container">
  <div class="wrapper col-md-4 col-md-offset-4 col-sm-10">
<div class="table-border">
    <h2>マイページ</h2>
    <br />

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= f.error_messages! %>

      各種設定の変更が出来ます。<br /><br />

      <div class="field">
        <%= f.label :メールアドレス %><br />
        <%= f.email_field :email, autofocus: true, class: "form-control" %>
      </div>
      <br>

      <!-- 名前入力用のフィールドを追加 -->
      <div class="field">
        <%= f.label :名前 %><br />
        <%= f.text_field :name, class: "form-control" %>
      </div>
      <br />

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>現在、確認中です。 for: <%= resource.unconfirmed_email %></div>
      <% end %>

      <div class="field">
        <%= f.label :新しいパスワード %><br />
        (変更しない場合は空白にしてください。)<br />
        <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
        <% if @minimum_password_length %>
          <%= @minimum_password_length %> 文字以上必須
        <% end %>
      </div>
      <br />

      <div class="field">
        <%= f.label :新しいパスワード（再入力） %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
      </div>
      <br />

      <div class="field">
        <%= f.label :現在のパスワード %><br />
        （変更承認のために必要です。）<br />
        <%= f.password_field :current_password, autocomplete: "off", class: "form-control" %>
      </div>
      <br />

      <div class="actions">
        <%= f.submit "変更する" %>
      </div>
    <% end %>
    <br />
</div>

<div class="table-border">
    <h3>アカウントの消去</h3>
    <br />

    <p>アカウント消去をご希望の方は、</p>
    <p>下記ボタンを押してください。 <%= button_to "アカウント情報を消去する", registration_path(resource_name), data: { confirm: "本当に消去してかまいませんか？" }, method: :delete %></p>

    <%= link_to "戻る", :back %>
</div>
  </div>
</div>
